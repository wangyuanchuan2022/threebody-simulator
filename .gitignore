# 本地专用：内嵌真实地形的构建变体（体积大，且地形模型为第三方素材，不入库）
index-terrain.html
terrain-test.html
stl-test.html

# 第三方地形素材：原始全分辨率模型不入库（体积大，可由 README 步骤自行获取）；
# 已降采样的 2K 版本入库，使 `node build.mjs` 能复现站点上发布的那份单文件。
assets/terrain.glb
assets/terrain.stl
!assets/terrain-2k.glb

# 测试过程产物（可由 tests/ 下脚本重新生成）
tests/*.png
tests/coverage/
tests/browser-coverage.json
tests/fixtures/
tests/star-bloom.json

# 评审与安全审查留档（内部材料，含本机路径）
review/
security-reports/

# 杂项
.tools/
*.log
